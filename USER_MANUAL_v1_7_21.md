# 人事报表智能体 — 用户手册

**版本**: v1.7.21  
**更新日期**: 2026年7月22日

---

## 目录

1. [系统概述](#1-系统概述)
2. [快速入门](#2-快速入门)
3. [运行日历](#3-运行日历)
4. [数据录入工作台](#4-数据录入工作台)
5. [人工确认](#5-人工确认)
6. [报表预览与发布](#6-报表预览与发布)
7. [历史归档](#7-历史归档)
8. [设置页面](#8-设置页面)
9. [常见问题与限制](#9-常见问题与限制)
10. [部署与运维](#10-部署与运维)

---

## 1. 系统概述

人事报表智能体是一个自动化报表生成平台，用于：

- 每日生成《员工数增减情况日报》
- 每周最后一个工作日生成周报
- 支持上传4类源文件：人员表、离职人员报表、协议签署/OA Release、招聘数据
- 通过AI视觉识别处理图片格式的OA和招聘数据
- 对不确定项提供人工确认机制
- 链式累计MTD（月累计）/YTD（年累计）数据

**访问地址**: `http://172.17.60.102:8080/`（当前部署服务器）

**浏览器要求**: Chrome、Edge、Firefox 最新版

---

## 2. 快速入门

### 2.1 完整操作流程

```
运行日历 → 点击日期 → 上传4类文件 → 确认识别结果 → 预览日报 → 发布
```

### 2.2 所需文件

| 序号 | 文件类型 | 格式 | 说明 |
|------|---------|------|------|
| 1 | 人员表 | Excel (.xls/.xlsx) | 在职人员与组织归属事实 |
| 2 | 离职人员报表 | Excel (.xls/.xlsx) | 离职流程与最后工作日 |
| 3 | 协议签署/OA Release | Excel 或 图片 | 协议解除与OA流程记录 |
| 4 | 招聘数据 | Excel 或 图片 | Offer与预计入职统计 |

**图片格式支持**: jpg, jpeg, png, bmp, webp, tif, tiff

> ⚠️ **注意**: 图片上传后由AI视觉模型识别，结果需人工核对确认。原始图片按安全策略不留存。

---

## 3. 运行日历

登录后进入运行日历页面，可以看到当前月份的日历视图。

### 3.1 日期状态

每个日期格子显示当前运行状态：

| 状态 | 含义 |
|------|------|
| 等待录入 | 该日尚未开始处理 |
| 处理中 | 文件正在解析 |
| 待确认 | 有识别结果待人工确认 |
| 可预览 | 所有步骤完成，可以预览报表 |
| 已发布 | 日报已发布 |
| 已合并 | 该运行已被合并到其他运行 |

### 3.2 操作

- **点击日期**: 进入已有运行的工作台
- **点击空白工作日**: 自动创建新运行并进入工作台
- **月份切换**: 使用左右箭头切换月份
- **点击"今天"**: 快速跳转到当天

---

## 4. 数据录入工作台

### 4.1 页面结构

工作台分为三个主要区域：

1. **输入数据区** — 上传4类源文件
2. **人工确认区** — 确认AI识别结果和系统决策
3. **运行摘要区** — 显示运行元数据

顶部步骤指示器：**数据录入 → 人工确认 → 报表预览 → 完成发布**

### 4.2 上传文件

1. 点击对应类型区域的"选择文件"按钮
2. 选择要上传的文件
3. 等待解析完成（显示绿色对勾表示成功）
4. Excel文件直接解析，图片文件会触发AI视觉识别

> ⚠️ **注意**: 当前系统为单线程处理。上传一个文件后，请等待其解析完成再上传下一个。同时上传多个文件可能导致处理冲突。

### 4.3 文件上传状态

| 显示 | 含义 |
|------|------|
| 绿色对勾 | 文件已成功上传并解析 |
| 加载动画 | 正在解析中 |
| 红色错误 | 上传或解析失败 |

### 4.4 输入锁定与同日修订

当运行状态进入"可预览"或"已合并"后，输入区域会被锁定。

如需更换文件：
1. 点击"创建同日修订Run"按钮
2. 系统创建新运行，跳转到新的工作台
3. 重新上传所有4类文件
4. 完成确认后，新运行将替代旧运行

---

## 5. 人工确认

### 5.1 确认类型

系统会在以下情况要求人工确认：

1. **图片识别确认** — OA/Release或招聘数据为图片格式上传时，AI识别结果需确认
2. **系统决策确认** — 周报计算中的并列项目选择等

### 5.2 确认操作

- 查看识别结果表格，核对数据
- 确认无误后点击"确认"按钮
- 如需重新上传，点击"替换输入"

### 5.3 状态提示

| 提示 | 含义 |
|------|------|
| N项待确认 | 还有需要确认的项目 |
| 无需确认 | 所有项目已处理 |
| 周报还有N项待复核 | 周报相关确认不影响日报 |

> ℹ️ **说明**: 日报所需的人工确认必须在预览前完成。周报的复核可以在预览页面中处理，不影响日报预览和发布。

---

## 6. 报表预览与发布

### 6.1 进入预览

在工作台点击"预览日报与周报"按钮，进入预览页面。

> ⚠️ **按钮可能禁用的原因**:
> - 有未确认的项目
> - 缺少上一份已发布日报基线
> - 运行状态尚未就绪

### 6.2 预览内容

**日报预览**: 显示Row2~Row40共39行数据，包含：
- 当日入职/离职 (Row2-3)
- MTD累计 (Row8-12)
- YTD累计 (Row13-17)
- 预估数据 (Row18-22)
- 在岗时长校验
- 与上一日报的差异对比

**周报预览**: 显示事业部×主体维度的人员统计

### 6.3 发布操作

- 预览确认无误后，点击"发布日报"按钮
- 日报发布后自动成为下一工作日的计算基线
- 周报仅在每周最后一个工作日可以发布

### 6.4 校验结果

预览页面显示校验清单，包括阻断(BLOCK)和复核(REVIEW)两个级别：
- **阻断未通过**: 必须处理，否则无法发布
- **复核待处理**: 建议处理，但不阻止发布

---

## 7. 历史归档

历史归档页面展示所有已发布的报告版本。

### 7.1 功能

- 按类型筛选（日报/周报）
- 查看报告版本和发布时间
- 下载Excel或执行说明文件
- 跳转到对应运行进行复核

---

## 8. 设置页面

### 8.1 系统状态

系统状态页面显示：
- MySQL数据库连接状态
- Redis缓存连接状态
- 数据库结构迁移状态
- 产物目录可写状态
- 生产配置完整性

### 8.2 配置项

可配置的选项（需管理员权限）：
- 规则版本号
- 报表生成行为参数

---

## 9. 常见问题与限制

### 9.1 运行相关

**Q: 为什么"预览日报与周报"按钮是灰色的？**  
A: 可能有以下原因：
1. 4类文件没有全部上传完成
2. 人工确认项目未处理
3. 缺少上一日已发布的日报基线（首次使用时）
4. 页面未刷新 — 请刷新浏览器页面

**Q: 同日修订Run需要重新上传所有文件吗？**  
A: 是的。同日修订会创建一个全新的运行，需要重新上传全部4类文件。

**Q: 运行记录无法打开？**  
A: 通常是网络连接问题或后端服务未启动，请检查后端状态。

### 9.2 数据相关

**Q: 为什么Row3（今日离职）比实际少？**  
A: 系统会自动去重。如同一自然人有多个工号，只计算当前有效工号的离职，避免重复计数。

**Q: 图片识别结果不准怎么办？**  
A: 点击"替换输入"重新上传，或转换为Excel格式上传（推荐）。

**Q: 之前发布的日报数据有误，能修改吗？**  
A: 在同日创建修订Run，上传修正后的源文件，重新预览发布即可。新版本会替代旧版本。

### 9.3 限制

- 系统为单线程处理，文件需逐个上传
- 图片上传后原始图片不留存，仅保留结构化识别结果
- 每日仅允许一份有效日报作为基线
- 周报仅可在本周最后一个工作日发布
- 单文件上传上限: 20MB

---

## 10. 部署与运维

### 10.1 系统架构

```
[浏览器] → [Nginx :8080] → [Uvicorn :8000] → [FastAPI]
                                      ↓
                              [MySQL 8.0] [Redis]
```

### 10.2 服务启停

**启动后端**:
```powershell
cd C:\Users\Administrator\Desktop\v1_7.21_new\backend
Start-Process -NoNewWindow python -ArgumentList "-m uvicorn app.main:app --host 127.0.0.1 --port 8000"
```

**重启Nginx**:
```powershell
cd C:\nginx
.\nginx.exe -s reload
```

**停止后端**:
```powershell
Get-Process -Name python -ErrorAction SilentlyContinue | Stop-Process -Force
```

### 10.3 健康检查

访问 `http://127.0.0.1:8000/health` 查看后端状态。正常返回：
```json
{"status":"ok","mysql":true,"redis":true,"llm_enabled":true}
```

### 10.4 数据库信息

- 数据库: MySQL 8.0
- 库名: ai_hr_reports
- 用户: hr_agent
- 连接地址: 127.0.0.1:3306

### 10.5 文件目录

| 目录 | 用途 |
|------|------|
| `v1_7.21_new\backend\` | 后端Python代码 |
| `v1_7.21_new\frontend\dist\` | 前端静态文件 |
| `v1_7.21_new\backend\.env` | 环境配置文件 |
| `C:\nginx\conf\nginx.conf` | Nginx配置 |

---

*本手册适用于人事报表智能体 v1.7.21 版本。如有疑问请联系系统管理员。*
