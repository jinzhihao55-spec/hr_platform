# 服务器缺陷、代码问题与部署注意事项汇总

> 基于 v1_7.20 实际部署、v1_7.21 完整代码审查，以及 2026-07-08 ~ 07-21 多轮对话中暴露的问题。
> 生成日期：2026-07-21

---

## 一、当前服务器（172.17.60.102:8080 / v1_7.20）已知缺陷

### 1.1 Windows Server 原生部署的脆弱性

服务以 uvicorn 直跑 + Nginx 反代方式运行在 Windows Server 上，没有容器化，没有进程守护。

| 问题 | 严重程度 | 说明 |
|------|---------|------|
| **无进程守护** | 高 | uvicorn 以 `python -m uvicorn` 在前台运行，终端关闭即停止。没有 systemd/WinSW/NSSM 包装为 Windows 服务。 |
| **无自动重启** | 高 | 若 Python 崩溃或内存溢出，服务不会自动恢复。需要手动登录服务器重新启动。 |
| **Nginx 与后端耦合在同一机器** | 中 | 没有 Docker 网络隔离，所有端口在机器本地暴露。虽然 Nginx bind 了 `0.0.0.0:8080`，但后端 8000 也可能被内网其他机器直接访问。 |
| **前端构建后无法热更新** | 低 | Vite dev server 不在生产环境运行，变更需 `npm run build` 后手动替换 `dist/`，没有 CI/CD。 |

### 1.2 依赖版本冲突

| 问题 | 表现 | 当前状态 |
|------|------|---------|
| **redis-py 8.0.1 vs Windows Redis 5.0** | `pip install redis` 装了最新版 8.0.1，与服务器上跑的 Redis 5.0 协议不兼容 | 已降级到 `redis>=4.0,<5.0`，但 pip 锁未固化（无 requirements.txt lock） |
| **无 lock 文件** | 若有人执行 `pip install -r requirements.txt` 可能再次拉入不兼容版本 | 需要生成 `requirements.lock` 或明确上限 |

### 1.3 SSH 不可用

OpenSSH.Server 在 Windows Server 上安装失败，报 Windows Update 错误 `0x8024401c`。这导致：

- 无法远程执行命令（跑测试、查看日志、重启服务）
- 无法 scp 传输文件（部署新版本只能靠 RDP 桌面拖文件）
- 所有运维操作必须通过 RDP 图形界面手动完成

### 1.4 无数据库备份机制

从代码和部署文档看，没有配置 MySQL 定时备份或 binlog 归档。如果数据库损坏或误操作，数据无法恢复。

### 1.5 旧路径接口的幂等性风险

v1_7.20 仅提供旧路径接口（`POST /ingest` + `POST /reports/daily`）。每天的数据是 UPSERT 写入可变主表的。如果同一天多次执行：

- 第二次 `POST /ingest` 会 UPDATE 已有行，但首次可见日期 (`first_visible_date`) 不会被回退
- 重复 `POST /reports/daily` 会用同一个 `report_date` 覆盖日报行——但 Python 代码中未见阻止重复生成的门禁
- 没有"运行"概念，无法溯源"这份日报是用哪批数据生成的"

### 1.6 图像识别依赖外部 LLM

协议签署和招聘数据如果是 PNG/JPG 截图，完全依赖 `LLM_VISION_API_KEY` 调用阿里云百炼。服务器上没有本地 OCR 兜底。如果：

- API Key 过期或额度用尽
- 网络不通
- 百炼服务故障

→ 图像类输入直接失败，无降级路径。

---

## 二、v1_7.21 代码审查中发现的缺陷与风险

### 2.1 旧路径与新路径数据源分裂

这是 v1_7.21 最大的设计风险。

- **旧路径**（`POST /ingest` → `POST /reports/daily`）：从可变主表 `employees`、`employee_resignations` 等读数据。
- **新路径**（`POST /runs` → `PUT /runs/{id}/sources/{type}` → `POST /runs/{id}/publish`）：从 Run 级事实表 `employment_facts`、`resignation_facts` 等读数据。

两条路径的数据源完全独立。如果用户在同一个日期混用——比如上午用旧路径跑了一版日报，下午用新路径创建了 Run——两份结果的 Row 值可能不同，且不会互相提示冲突。

**改进建议**：至少在 `/reports/daily` 加入一个检查：如果该日期已有 Run 且已发布，发出警告。

### 2.2 PERSON_KEY_SECRET 的生产必填约束

`config.py` 第 113-118 行：

```python
if self.app_env not in _NON_PROD_ENVS and not self.person_key_secret:
    raise ValueError(...)
```

当前服务器以 `APP_ENV=dev` 运行绕过了这个校验。如果要切到 `prod`，必须设置 `PERSON_KEY_SECRET`，且：

- 这个 key 一旦设定就不能改——改了会导致所有历史人员的 `person_key` 值变化，跨 Run 去重失效。
- 部署文档（DEPLOYMENT_SINGLE_USER.md）明确写了"恢复环境必须沿用原值"，但如果运维人员不知道这一点，部署恢复时重新生成一个就会破坏数据。

### 2.3 初始基线冻结的启动顺序依赖

部署 v1_7.21 到已有数据的数据库后，旧数据在 `employees` 等表中，但新路径需要 `employment_facts` 等 Run 级表有数据。必须通过 `POST /runs/{run_id}/baseline`（上传已验收日报 xlsx）来冻结初始基线。如果这一步被遗漏或顺序错误：

- 新 Run 找不到 baseline_report_id，计算链式基线会报 `baseline_missing`
- 无法从日历正常创建后续工作日的 Run

### 2.4 四个新 Migration 的重复执行安全性

v1_7.21 新增了 6 张 Run 相关表（report_runs、run_sources、run_report_targets、employment_facts、resignation_facts、release_facts、recruitment_snapshots、fact_events、run_decisions、run_validations），以及 published_reports、report_artifacts、publication_attempts，还有 `snapshot_json` 字段从 TEXT 扩到 LONGTEXT 的 migration。

SQL 用了 `CREATE TABLE IF NOT EXISTS` 和 `ALTER TABLE ... MODIFY COLUMN` 做幂等，但如果 v1_7.20 之前已经执行过部分 migration（比如之前部署时报错中途停止），可能出现部分表存在但结构不一致的情况。迁移脚本没有版本号追踪机制。

### 2.5 图像识别无本地降级

`image_parser.py` 已完全移除 PaddleOCR。如果视觉 LLM 不可用，整个图像处理链路失败。对于 Excel 文件不受影响——但对于本项目的测试数据，4 类输入中有 2 类（协议签署、招聘数据）在各日期目录中是 PNG/JPG 图像，不是 Excel。这意味着没有视觉 LLM 就无法跑完整的 9 天回归测试。

### 2.6 FactBundle 的 OOM 风险

`FactBundleService.build()` 将 Run 的全部事实（所有员工的 employment_fact、所有的 resignation_fact、release_fact、recruitment_snapshot、fact_event）一次性加载到 pandas DataFrame 中，不做分页或流式处理。如果 Run 覆盖一个 500+ 人的大型 BU，每条事实都有跨月累计的 first_visible 日期 JSON，内存占用可能很大。

### 2.7 周报部门常量硬编码

`constants.py` 中 `WEEKLY_BUSINESS_UNIT_ORDER` 固定了 8 个部门槽位（NINS/NENT/NITL/NMSI/NBJO/NWTS/NWMT/NGOV），`WEEKLY_PROJECT_FAMILIES` 固定了 6 个项目族。如果有新部门或新项目加入，需要改代码而非改配置。

### 2.8 .env 中 API_AUTH_TOKEN 为空

当前 v1_7.21 的 `.env` 中 `API_AUTH_TOKEN=` 是空值。这意味着 `/api/` 所有接口无认证门禁，任何人只要能访问 8000 端口就能调用所有接口（包括上传文件、生成日报、删除数据等）。部署文档要求生产环境必须设置，但因为 APP_ENV 也是 dev，不会启动报错。

---

## 三、部署上需要注意的点

### 3.1 v1_7.20 → v1_7.21 升级顺序

严格按以下顺序操作，否则数据会坏：

1. **备份数据库**：`mysqldump ai_hr_reports > backup_$(date +%Y%m%d).sql`
2. **停止后端服务**：杀掉 uvicorn 进程
3. **执行新 migration**：按日期顺序执行 4 个 SQL 文件
   - `2026-07-15_add_report_runs.sql`
   - `2026-07-15_add_run_facts.sql`
   - `2026-07-15_add_publications.sql`
   - `2026-07-16_expand_publication_snapshot.sql`
4. **配置 .env**：
   - 设置 `PERSON_KEY_SECRET`（用 `secrets.token_urlsafe(48)` 生成，保存到安全位置）
   - 设置 `API_AUTH_TOKEN`（同上）
   - 确认 `LLM_VISION_API_KEY` 与 `LLM_API_KEY` 一致（阿里云百炼同一个 key 可同时用于文本和视觉）
5. **替换后端代码**：将 v1_7.21/backend 覆盖到 v1_7.20/backend
6. **构建并替换前端**：`cd v1_7.21/frontend && npm run build`，将 dist/ 复制到 Nginx 根目录
7. **更新 Nginx 配置**：确保 `proxy_set_header X-API-Token` 与新的 `API_AUTH_TOKEN` 一致
8. **启动后端**：`python -m uvicorn app.main:app --host 0.0.0.0 --port 8000`
9. **验证 health**：`curl http://127.0.0.1:8080/api/health`
10. **冻结初始基线**：在前端创建一个 Run，上传已验收的日报 xlsx 文件，调用 `POST /runs/{run_id}/baseline`

### 3.2 必须持久化保存的配置项

以下一旦生成就不能丢，丢了数据就不可恢复：

| 配置项 | 后果 |
|--------|------|
| `PERSON_KEY_SECRET` | 丢失后所有人员的 person_key 改变，跨 Run 去重和 first-visible-date 追踪全部失效 |
| `MYSQL_PASSWORD` | 数据库无法连接 |
| `API_AUTH_TOKEN` | 前端无法调用后端接口（Nginx 注入该 token） |
| MySQL 数据库文件 | 所有历史日报、周报、Run、事实数据 |

建议：将这些写入一个独立的加密文件，离线存储，并在部署文档中注明恢复步骤。

### 3.3 Windows 服务化

当前缺少 `nssm` 或 `WinSW` 包装。推荐方案：

```powershell
# 用 NSSM 将 uvicorn 注册为 Windows 服务
nssm install HRAgentAPI "C:\Python310\python.exe" "-m uvicorn app.main:app --host 127.0.0.1 --port 8000"
nssm set HRAgentAPI AppDirectory "C:\Users\Administrator\Desktop\v1_7.21\backend"
nssm set HRAgentAPI Start SERVICE_AUTO_START
nssm start HRAgentAPI
```

这样即使服务器重启，后端也会自动拉起。

### 3.4 Redis 版本兼容

在 Windows Server 安装的 Redis 是 5.0（Windows 移植版最高到此版本）。`pip install redis` 必须锁版本：

```
redis>=4.0,<5.0
```

如果不小心升级到 5.0+，会出现协议不兼容错误。

### 3.5 前端构建的环境变量

前端 `.env.production` 中不需要也不应该写 `API_AUTH_TOKEN`——这个 token 由 Nginx 在服务端注入 `X-API-Token` 头。前端代码中通过 `import.meta.env.VITE_API_BASE_URL` 决定 API 地址，构建时应该设为空或 `/`，让浏览器走同源请求。

### 3.6 LLM API Key 的风控

阿里云百炼的 API Key 是有 QPS 限制和月度额度限制的。当前代码中 LLM timeout 是 120 秒，max_retries 是 2。如果图像识别任务量大（每个日期 2 张图），加上解析/排障的 LLM 调用，可能会触发限流。需要在百炼控制台确认额度是否充足。

### 3.7 测试数据的隐私问题

`/Users/jzh/jzh/2026实习/data/学生测试集/` 是学生提供的数据。虽然名字可能已脱敏（使用"张三"、"李四"等化名），但在服务器上跑测试时：

- 临时文件会留在 `data/uploads/` 目录
- OCR 结果可能缓存或记录在日志中
- .env 中的 LLM_API_KEY 使得数据会经过阿里云百炼服务器

测试完成后应清理所有临时文件和日志。发布清单中明确要求"仓库测试只有假值，真实汇总值不写入断言"。

### 3.8 CORS 配置

当前 `.env` 中 `CORS_ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173` 只允许 Vite dev server 的地址。在部署到 Nginx（同源）时，CORS 不需要额外配置。但如果有人通过 IP 直接访问后端 8000 端口（绕过 Nginx），CORS 会报错。生产环境应该收紧这个白名单或设为空（同源不需要 CORS）。

---

## 四、基础设施问题（与 Claude 协作相关）

### 4.1 Bash 沙箱持续不可用

Claude Desktop 的 Linux 沙箱在启动时下载镜像失败，所有远程 shell 命令无法执行。这意味着 Claude 不能直接在 VM 里跑 Python 脚本、pip install、curl 测试接口等。

**排查方向**：检查 `/Users/jzh/Library/Logs/Claude/` 下的日志，看具体是哪个 CDN/URL 下载失败，是否需要配置代理。

### 4.2 无法访问内网地址

`172.17.60.102` 是私有 IP，Claude 的 `WebFetch`/`WebSearch` 工具安全策略禁止访问。所以不能直接从 Claude session 调用服务器 API 或 screenshot。

### 4.3 Playwright MCP 未连接

之前创建的 `claude_desktop_config_merged.json` 包含了 playwright MCP server，但可能没有被复制到 `~/Library/Application Support/Claude/claude_desktop_config.json` 并重启。

### 4.4 Windows Server 的 OpenSSH 安装失败

Windows Update 错误 `0x8024401c` 意味着服务器无法连接到微软更新服务器。这可能是因为服务器在内网，没有配置 Windows Update 代理或 WSUS。

替代方案：
- 使用 `winrm`（PowerShell Remoting）代替 SSH
- 手动下载 OpenSSH 的 `.cab` 文件离线安装
- 安装第三方 SSH 服务如 Bitvise SSH Server

---

## 五、立即优先级排序

| 优先级 | 事项 | 类别 |
|--------|------|------|
| P0 | 数据库备份（mysqldump） | 运维 |
| P0 | 确认 PERSON_KEY_SECRET 和 API_AUTH_TOKEN 已生成并安全保存 | 配置 |
| P1 | 用 NSSM/WinSW 将 uvicorn 注册为 Windows 服务 | 运维 |
| P1 | 锁死 redis 版本上限，生成 requirements lock | 依赖 |
| P2 | 修复 SSH 或配置 WinRM | 运维 |
| P2 | 验证 4 个 migration SQL 在执行前无冲突 | 部署 |
| P2 | 确认阿里云百炼 API 额度足够 9 天回归测试 | 资源 |
| P3 | 部署 v1_7.21 并冻结初始基线 | 部署 |
| P3 | 跑 9 天回归测试 | 测试 |
| P3 | 配置 Claude Desktop MCP | 开发工具 |
