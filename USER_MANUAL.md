# HR 报表智能体 v1.7.21 — 用户手册

## 1. 系统概述

人事报表智能体是一个**确定性计算**的日报/周报生成系统。所有数字来自输入数据的计算和数据库查询，不由 LLM 产生。

### 核心概念

| 概念 | 说明 |
|------|------|
| **Run（运行）** | 一个工作日的完整处理单元，包含 4 类数据源、决策、预览、发布 |
| **源文件** | 4 类输入：人员表、离职报表、协议签署、招聘数据 |
| **决策** | 系统不确定的判断（如 OCR 确认、数量补充），需人工确认 |
| **基线** | 上一份已发布日报的累计值（Row8/9/13/14/30），用于链式计算 |
| **发布** | 将预览结果冻结为不可变报告版本 |

---

## 2. 完整操作流程

### 第一步：创建运行

打开运行日历 → 点击空白工作日 → 系统自动创建 Run

### 第二步：上传 4 类数据源

| 入口 | 接受格式 | 说明 |
|------|---------|------|
| 人员表 | .xlsx / .xls | 在职人员花名册（含工号、姓名、事业部、入离职日期等） |
| 离职人员报表 | .xlsx / .xls | 离职流程记录（含流程单号、工号、最后工作日） |
| 协议签署 / OA Release | .xlsx / .xls / PNG / JPG | OA 协议解除记录；图片需视觉 LLM 识别 |
| 招聘数据 | .xlsx / .xls / PNG / JPG | Offer 与预计入职统计；图片需视觉 LLM 识别 |

**支持拖拽上传**，直接将文件拖入对应区域即可。

### 第三步：人工确认

系统自动分析后显示待确认项目：
- **OCR 识别确认**：上传图片时，确认 AI 识别的结构化结果
- **数值补充**：招聘数据识别不完整时手动输入数值

可点击「全部确认」批量处理同类决策。已确认的决策可以点击撤销重新选择。

### 第四步：预览与发布

点击「预览日报与周报」→ 查看日报 Row2–Row40 各行数值 → 确认无误后点击「发布日报」。

**周报仅在每周最后一个工作日发布**（通常是周五），其余时间显示"未到周报日"。

### 第五步：查看与下载

发布后可在「日报」「周报」页面查看历史记录，下载 Excel 或执行说明。

---

## 3. 规范与限制

### 3.1 不可变性

- **已发布的报告不可修改**。这是设计原则——保证审计可追溯。
- **已冻结的 Run 输入不可替换**。输入一旦完成全部上传和确认即冻结（source_bundle_hash 生成）。

### 3.2 同日修订

需要修改已发布报告时：
1. 在 Run 工作区点击"创建同日修订 Run"
2. 重新上传全部 4 类源文件
3. 确认 → 预览 → 发布
4. 新版本会覆盖旧版本（旧版标记为"已替代"）

**注意**：同日修订 Run 不会自动继承前一个 Run 的源文件，需要手动重新上传全部 4 类。

### 3.3 链式基线

- 日报的 MTD/YTD 累计值（Row8/9/13/14/30）依赖于**上一份已发布日报的基线**
- 发布日报 A 后，下一工作日日报 B 自动引用 A 作为基线
- 首次使用无历史基线时，系统自动以全 0 启动

### 3.4 图片识别

- 协议签署和招聘数据支持图片上传（PNG/JPG）
- 图片识别依赖视觉 LLM（阿里云百炼 qwen3.7-plus）
- 未配置视觉 LLM 时，图片上传将被拒绝，请改用 Excel
- 识别结果仅供参考，需人工确认后参与计算

### 3.5 文件格式要求

| 源类型 | 必含列 |
|--------|--------|
| 人员表 | 工号、中文名/英文名、员工类型、员工状态、入职日期、离职日期、事业部、项目等 |
| 离职报表 | 流程单号、工号、流程状态、最后工作日、离职方式等 |
| 协议签署 | 单号、流程类型/名称、当前状态、申请时间等 |
| 招聘数据 | 含动态月份列（如"X月接受offer在Y月即将入职"） |

### 3.6 系统依赖

| 组件 | 要求 |
|------|------|
| MySQL | 8.0+ |
| Redis | 5.0（Windows 最高支持版本） |
| Python | 3.12+ |
| LLM API | 阿里云百炼（文本：qwen3.7-plus；视觉：qwen3.7-plus） |
| 浏览器 | Chrome/Edge 最新版 |

---

## 4. 常见问题

**Q: 预览按钮是灰色的？**
鼠标悬停按钮可看到具体原因：未完成上传、有未确认决策、缺少基线等。

**Q: 图片上传后一直转圈？**
视觉 LLM 识别需要 1–5 分钟，请耐心等待。如超时报错，请改用 Excel 格式。

**Q: "缺少上一份已发布日报基线"？**
首次使用系统时的正常提示。系统会自动以全 0 基线启动，发布第一份日报后即可建立基线。

**Q: 周报按钮显示"未到周报日"？**
周报仅在每周最后一个工作日（通常周五）发布。其他日期可预览但不可发布。

**Q: 数字不对怎么办？**
在「计算日志」页面可逐行追溯每个 Row 的取数公式和来源。

**Q: 如何撤销已确认的决策？**
在决策详情中点击撤销按钮（需后端已部署 v1.7.21+）。

---

## 5. 版本历史

| 版本 | 日期 | 主要变更 |
|------|------|---------|
| v1.7.20 | 2026-07 | 可变主表模式，单路流水线 |
| v1.7.21 | 2026-07-21 | Run 工作流架构，不可变报告，初始基线自动启动，决策撤销，批量确认，拖拽上传，文件名回显，全局 Toast |
