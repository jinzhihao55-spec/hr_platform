# 单用户 V1 发布清单

## 代码与测试

- [ ] 后端全量 `pytest` 通过
- [ ] 假数据发布回归 12/12 通过
- [ ] 前端测试、lint、生产构建通过
- [ ] Docker Compose 配置展开无错误，`web/api/migrate/mysql/redis` 均健康
- [ ] 通过 Nginx 实际访问 `/api/ready`、日历、Run 和报表接口

## 业务验收

- [ ] 首次使用可导入前一工作日已验收日报作为正式基线
- [ ] 正式入口仅包含人员表、离职人员报表、OA/Release、招聘数据
- [ ] 日报按日追加累计列，历史列、样式、合并单元格和在岗时长表保持不变
- [ ] 同一自然人的重复工号不重复计入入职/在职人数
- [ ] 滚动快照继承上一已发布 Run 的首次可见日期；截点后出现的事实计入下一报告日
- [ ] 缺少 LWD 的 OA 按 Q5 只计 Row5，不进入 Row30
- [ ] 周报值、样式、项目归并和日报合计勾稽通过
- [ ] 预览与发布 Excel 回读一致，日报和周报可独立阻断/发布

## 数据保护

- [ ] Git 状态中没有真实输入、结构化 OCR、生成报表、日志或 trace
- [ ] 仓库测试只有假值；真实汇总值不写入断言
- [ ] 基础隐私扫描为零发现
- [ ] 使用导师本机敏感值清单的精确扫描为零发现
- [ ] 浏览器 bundle 不包含 API token、数据库密码或人员 HMAC secret
- [ ] 临时 OCR、临时工作簿和诊断目录已清除

## 配置与运维

- [ ] `deploy/.env` 的 MySQL、Redis、API token 和 `PERSON_KEY_SECRET` 均为独立强随机值
- [ ] `PERSON_KEY_SECRET` 已纳入备份且恢复时保持不变
- [ ] 仅 Nginx 绑定 `127.0.0.1`；MySQL、Redis、API 不直接暴露
- [ ] 数据库迁移容器可重复执行且成功退出
- [ ] 已验证备份、恢复、磁盘空间和发布中断恢复流程
- [ ] OCR key 未硬编码；未配置时图片输入明确阻断，Excel 路径仍可用

## 发布证据

- [ ] 记录后端、前端和数据库三个仓库的 commit SHA
- [ ] 保存 Compose 镜像摘要和验证时间
- [ ] 保存仓库外的脱敏 `regression_summary.json`
- [ ] 记录已验收日期范围、日报数量、周报数量和结果，不记录业务值
- [ ] 记录剩余限制：V1 单用户、月报未实现、OCR 结果仍需人工确认
