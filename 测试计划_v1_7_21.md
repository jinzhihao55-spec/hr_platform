# v1_7.21 代码审查与测试计划

## 1. v1_7.21 架构变更概览

v1_7.20（当前运行在 172.17.60.102:8080）使用 **可变主表**（employees/employee_resignations/oa_protocols/recruitment_pipeline），每天 Upload → UPSERT → Generate daily, each day's report chains from previous day's baseline.

v1_7.21 引入了 **Run 工作流**架构，是重大设计变更：

### 新增核心概念
- **ReportRun**: 一次完整报表运行，包含四个数据源、版本号、baseline_report_id
- **FactBundle**: 不可变的、Run 级别的事实集合（EmploymentFact/ResignationFact/ReleaseFact/RecruitmentSnapshot/FactEvent）
- **Run 状态流转**: created → parsing → needs_review → ready → deduplicated/failed
- **Preview/Publish**: 预览通过验证后才可发布，发布后写入 PublishedReport
- **First-visible date resolution**: 跨 Run 追踪每条事实何时首次在系统中出现（支持"晚到补入"逻辑）
- **Employment carry-forward**: 人员快照可能漏掉上周最后一天离职的员工，从上一个 Run 回补

### 两条计算路径共存

**旧路径**（v1_7.20 路径，被保留）:
```
POST /ingest → ExtractionAgent → mutable tables (UPSERT)
POST /reports/daily → CalculationAgent → daily_reports table (chain from previous day)
POST /reports/weekly → CalculationAgent → weekly_reports table
```

**新路径**（v1_7.21 新增）:
```
POST /runs → creates ReportRun
PUT /runs/{id}/sources/{type} → RunSourceService.ingest → Run-scoped fact tables
POST /runs/{id}/parse → materialize_run_history → resolve first-visible dates
GET /runs/{id}/preview/daily → FactBundleService.build → CalculationAgent.run_daily_bundle → validators
POST /runs/{id}/publish → publication_service → PublishedReport + ReportArtifact
```

## 2. 关键风险与可能的问题

### 2.1 旧路径仍可用但可能有兼容问题
- `POST /ingest` 仍调用 ExtractionAgent，这个 Agent 已支持 `tmp_dir` 参数
- 但是 `POST /reports/daily` 的 `generate_daily` 函数仍从 **可变主表** 读取（`report_repo.load_employees` 等），不是从 Run 的 fact tables
- 如果新旧路径混用，新路径发布了 Run 但旧路径重新 generate daily，数据可能不一致

### 2.2 PERSON_KEY_SECRET 缺失
- v1_7.21 的 `FactBundle` 和 `RunSourceService` 依赖 `derive_person_identity` 函数来稳定标识自然人
- 这个函数需要 `PERSON_KEY_SECRET`，在 `.env.example` 中为空
- 如果缺失，部署在非 dev/local/test 环境时 `_validate_secrets` 会阻止启动
- 但在 dev 模式下，空 secret 可能导致身份字段为 None，影响跨期去重

### 2.3 fact_history_service 的 initial baseline 路径
- 当前服务器上已经有数据在 `employees` 表中，如果升级到 v1_7.21，需要先通过 `POST /runs/{run_id}/baseline` (finalize_initial_run_baseline) 冻结已有数据为初始基线
- 这需要上传已验收的 daily xlsx 文件

### 2.4 图像识别的视觉 LLM 依赖
- `image_parser.py` 完全依赖 LLM_VISION_* 配置（阿里云百炼 qwen3.7-plus）
- 已不再支持 PaddleOCR 本地识别
- 当前服务器上的 LLM_API_KEY 已填写，但 LLM_VISION_API_KEY 是否配置需要验证

### 2.5 招聘数据月份动态识别
- `parsers.py` 中 `parse_recruitment` 通过正则识别动态月份列：如 "5月接受offer在6月即将入职"
- 真实招聘截图中表头格式可能不同（如 "6月已入职人数"、"6月待入职人数" 等），需要视觉 LLM 正确识别
- 如果识别结果与正则不匹配，Row38/Row39 的值可能为 None

### 2.6 周报代码的部门数量约束
- `constants.py` 中 `WEEKLY_BUSINESS_UNIT_ORDER` 固定了 8 个部门
- `_dedupe_active_people` 和 `_cost_center_rows` 等函数依赖固定的 `WEEKLY_PROJECT_FAMILIES`
- 测试数据中有自定义的项目名（与配置不匹配时，周报会正确归并但不一定完全匹配预期）

### 2.7 validators 的硬阻断
- `run_daily_checks` 有 15 个检查项，其中 13 个是硬阻断
- 三个关键阻断：公式链、在岗时长 B10=Row14、招聘取数一致性
- 如果 B10 和 Row14 差 1，整个日报都会被阻断

## 3. 测试数据预期结果摘要

根据测试数据中的 `日报执行说明`，以下是关键日期的预期值：

### 日报基准：7/8 是测试序列中的关键日（第一个有实际离职事实的日期）

| 日期 | Row2 | Row3 | Row4 | Row5 | Row8 | Row9 | Row13 | Row14 | Row30 | Row31 | Row32 | Row37 | Row38 | Row39 | Row40 |
|------|------|------|------|------|------|------|-------|-------|-------|-------|-------|-------|-------|-------|-------|
| 7/8  | 0 | 3 | 2 | 0 | 26 | 5 | 185 | 128 | 0 | 4 | 3 | 26 | 0 | 0 | 26 |
| 7/9  | 0 | 0 | 0 | 0 | 26 | 5 | 185 | 128 | 0 | 4 | 3 | 26 | 0 | 0 | 26 |
| 7/10 | 0 | 0 | 0 | 0 | 26 | 5 | 185 | 128 | 0 | 4 | 3 | 26 | 0 | 7 | 33 |
| 7/13 | 12 | 0 | 0 | 0 | 38 | 5 | 197 | 128 | 0 | 4 | 3 | 38 | 0 | 7 | 45 |
| 7/14 | 0 | 1 | 1 | 0 | 38 | 6 | 197 | 129 | 0 | 6 | 2 | 38 | 0 | 3 | 41 |
| 7/15 | 1 | 2 | 0 | 0 | 39 | 8 | 198 | 131 | 0 | 6 | 2 | 39 | 0 | 3 | 42 |
| 7/16 | 0 | 0 | 2 | 0 | 39 | 8 | 198 | 131 | 0 | 8 | 2 | 39 | 0 | 3 | 42 |
| 7/17 | 0 | 1 | 0 | 0 | 39 | 9 | 198 | 132 | 0 | 8 | 2 | 39 | 0 | 3 | 42 |
| 7/20 | 3 | 3 | 1 | 0 | 42 | 12 | 201 | 135 | 0 | 8 | 2 | 42 | 0 | 0 | 42 |

### 日报公式链验证点（每日期都必须通过）:
- Row6=Row4+Row5, Row7=Row2-Row3
- Row8=昨日Row8+今日Row2, Row9=昨日Row9+今日Row3
- Row12=Row8-Row9-Row10+Row11
- Row13=昨日Row13+今日Row2, Row14=昨日Row14+今日Row3
- Row17=Row13-Row14-Row15+Row16
- Row33=Row30+Row31+Row32, Row19=Row33
- Row37=Row8, Row40=Row37+Row38+Row39, Row18=Row40
- Row22=Row18-Row19-Row20+Row21
- 在岗时长 B10 = Sheet1 Row14

### 周报预期（需要在最后一个工作日生成）

**W28 (7/10)**: 在职 479, 本周入职 22, 本周离职 3
**W29 (7/17)**: 在职 488, 本周入职 13, 本周离职 4

## 4. 测试执行建议

### 4.1 前提条件
1. 服务器上有可用的 MySQL (ai_hr_reports) 和 Redis
2. `.env` 文件中 LLM_API_KEY 和 LLM_VISION_API_KEY 已配置（用于图像识别）
3. PERSON_KEY_SECRET 已设置（用于自然人身份稳定标识）
4. 后端 Python 依赖已安装、前端已构建

### 4.2 测试顺序

**模式 A: 旧路径测试（快速验证计算引擎）**
1. 按日期顺序每天依次: 上传四源 → 生成日报
2. 验证每日日报的 Row 值与预期一致
3. 在最后一个工作日验证周报

**模式 B: 新路径测试（完整 Run 工作流）**
1. 先找一个第一天（如 7/8），创建 Run，上传四源，parse，resolve decisions
2. 建立初始基线（POST /runs/{run_id}/baseline，上传 7/8 的定稿日报）
3. 后续每天: 创建 Run（以已发布日报为 baseline）→ 上传四源 → parse → preview → 检查 validation → publish
4. 如果基础数据不全导致 baseline_missing，先解决澄清

### 4.3 关键测试点

1. **图像识别**：OA 协议签署截图和招聘数据截图的 OCR 准确性
2. **首次可见日期**：检查晚到补入逻辑是否正确（如 7/8 Row3=3 是因为田博旭/张谦/管依香的离职日期在 7/3、7/7、7/8 但今天才首次在人员表中可见）
3. **链式基线**：每天生成的日报是否被正确保存为下一天的基线
4. **在岗时长校验**：B10=Row14 是否通过（如果差 1，需要排查离职员工是否被正确统计）
5. **周报与日报交叉校验**：周报入离职合计是否等于日报 Row2/Row3 的周合计
6. **重复身份去重**：龚士峻 127324/127326 双工号是否只计一人

## 5. 测试脚本框架

### 5.1 依赖安装（在新环境中）

```bash
pip install pandas openpyxl xlrd requests python-dotenv --break-system-packages
```

### 5.2 旧路径测试脚本伪代码

```python
import requests
from datetime import date, timedelta

BASE = "http://172.17.60.102:8080/api"
HEADERS = {}  # 如 API_AUTH_TOKEN 已配，加 X-API-Token

def upload_ingest(report_date, employees, resignations, agreements, recruitment):
    """POST /ingest - 上传四源"""
    files = {}
    if employees: files["employees"] = open(employees, "rb")
    if resignations: files["resignations"] = open(resignations, "rb")
    if agreements: files["agreements"] = open(agreements, "rb")
    if recruitment: files["recruitment"] = open(recruitment, "rb")
    data = {"report_date": report_date}
    return requests.post(f"{BASE}/ingest", files=files, data=data, headers=HEADERS)

def generate_daily(report_date):
    """POST /reports/daily"""
    return requests.post(f"{BASE}/reports/daily", json={
        "report_date": report_date
    }, headers=HEADERS)

def get_daily_view(report_date):
    """GET /reports/daily/{date}/view"""
    return requests.get(f"{BASE}/reports/daily/{report_date}/view", headers=HEADERS)
```

### 5.3 预期检查项

```python
def verify_daily_rows(view, expected):
    for row_num, expected_val in expected.items():
        actual = view["rows"].get(str(row_num), {}).get("value")
        assert actual == expected_val, f"Row{row_num}: expected {expected_val}, got {actual}"

def verify_validation(view):
    failures = [v for v in view.get("validations", []) if v.get("hard_block") and not v.get("passed")]
    assert not failures, f"Hard validations failed: {failures}"
```

## 6. 已知部署差距

根据之前的部署记录，当前 172.17.60.102 上运行的是 v1_7.20。如果要部署 v1_7.21：

1. 需要执行新的 4 个 migration SQL
2. 需要设置 PERSON_KEY_SECRET
3. 需要 LLM_VISION_API_KEY（如果当前没有单独配置，可以使用与 LLM_API_KEY 相同的 key）
4. 数据库中新 Run 相关表需要创建（已在 migrations 中）
5. 旧的数据（employees 表等）仍然保留并可供旧路径使用

## 7. 测试策略建议

由于测试数据集中包含：
- 7/8, 7/9, 7/10, 7/13, 7/14, 7/15, 7/16, 7/17, 7/20 共 9 个工作日的完整四源数据
- 对应的日报执行说明（含预期 Row 值）
- W28 和 W29 两份周报执行说明
- 部分日期有 PNG/JPG 图像输入

建议：
1. **先在当前服务器上验证旧路径**：每天上传 → 生成日报 → 对比预期 → 检查差异
2. **如果旧路径验证通过，再部署 v1_7.21 测试新路径**
3. **优先使用 Excel 文件而非图像**（避免视觉 LLM 的不确定性），图像识别测试单独进行
